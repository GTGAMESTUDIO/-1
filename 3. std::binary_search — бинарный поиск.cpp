Зачем: Поиск в отсортированном массиве за O(log n).


bool found = std::binary_search(v.begin(), v.end(), 3); // true
Связка с:

std::lower_bound (первый элемент ≥ X).

std::upper_bound (первый элемент > X).
